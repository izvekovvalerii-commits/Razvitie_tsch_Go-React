# ✅ Фикс фильтров в Kanban

## 🔴 Проблема

**Что было:**
- Фильтры "ВСЕГО", "В РАБОТЕ", "АУДИТ" не влияли на Kanban представление
- В KanbanBoard были **локальные фильтры** (searchQuery, filterMP), не связанные с глобальными
- Дублирование логики фильтрации - одна в `useProjectsData`, другая в `KanbanBoard`
- Поиск в Kanban не синхронизировался с основным поиском страницы

## ✅ Решение

### 1. Убрал дублирование фильтров
**Было:**
```tsx
// В KanbanBoard - локальный state ❌
const [searchQuery, setSearchQuery] = useState('');
const filteredProjects = useMemo(() => {
    return projects.filter(p => {
        const matchesSearch = !searchQuery || ...
        return matchesSearch && matchesMP;
    });
}, [projects, searchQuery, filterMP]);
```

**Стало:**
```tsx
// В KanbanBoard - используем глобальные фильтры ✅
interface KanbanBoardProps {
    searchQuery?: string;
    onSearchChange?: (query: string) => void;
}

// Фильтрация выполняется в useProjectsData
const filteredByMP = useMemo(() => {
    if (!filterMP) return projects; // projects уже отфильтрованы
    return projects.filter(p => p.mp === filterMP);
}, [projects, filterMP]);
```

### 2. Связал Kanban с глобальными фильтрами

**Projects.tsx:**
```tsx
<KanbanBoard
    projects={filteredProjects}  // Уже применены все фильтры из useProjectsData
    onStatusChange={updateStatus}
    onDelete={handleDeleteProject}
    searchQuery={filters.searchQuery}           // ✅ Передаем state
    onSearchChange={setFilters.setSearchQuery}  // ✅ Передаем setter
/>
```

### 3. Исправил панель поиска в Kanban

**Было:**
```tsx
<input 
    value={searchQuery}
    onChange={(e) => setSearchQuery(e.target.value)}  // ❌ Локальный state
/>
<button onClick={() => { setSearchQuery(''); ... }}>  // ❌ Локальный state
```

**Стало:**
```tsx
<input 
    value={searchQuery}
    onChange={(e) => onSearchChange?.(e.target.value)}  // ✅ Глобальный setter
/>
<button onClick={() => { onSearchChange?.(''); ... }}>  // ✅ Глобальный setter
```

---

## 🎯 Как теперь работает

### Поток данных:
```
useProjectsData (хук)
  ↓ управляет
Global Filters State
  ├─ quickFilter: 'all' | 'active' | 'audit'
  ├─ searchQuery: string
  ├─ selectedType: string
  └─ selectedStatus: string
  ↓ применяет фильтры
filteredProjects
  ↓ передает в
KanbanBoard
  ↓ дополнительно фильтрует
По МП (filterMP - локальный в Kanban)
  ↓ группирует
По статусам (columns)
```

### Работа быстрых фильтров:

**1. Клик "ВСЕГО" → `setQuickFilter('all')`**
```tsx
// В useProjectsData.ts
if (quickFilter === 'active') {
    result = result.filter(p => !['Слетел', 'Закрыт', 'Архив'].includes(p.status));
} else if (quickFilter === 'audit') {
    result = result.filter(p => p.status === 'Аудит');
}
// quickFilter === 'all' => не фильтруем
```

**2. Клик "В РАБОТЕ" → `setQuickFilter('active')`**
- Отфильтровываются проекты НЕ в статусах: "Слетел", "Закрыт", "Архив"

**3. Клик "АУДИТ" → `setQuickFilter('audit')`**
- Показываются только проекты со статусом "Аудит"

---

## 📊 Результаты

### ✅ Что теперь работает:

1. **Быстрые фильтры синхронизированы**
   - Клик "ВСЕГО"/"В РАБОТЕ"/"АУДИТ" влияет на все режимы (Grid, Table, Kanban)

2. **Поиск синхронизирован**
   - Ввод в поиск Kanban обновляет глобальный searchQuery
   - Работает в Grid/Table тоже

3. **Фильтры комбинируются**
   - Можно выбрать "В РАБОТЕ" + искать по ГИСу + фильтр по типу

4. **Дополнительный фильтр МП в Kanban**
   - Локальный фильтр по менеджеру проекта (только для Kanban)

---

## 🔧 Измененные файлы

### 1. `components/KanbanBoard.tsx`
- ✅ Добавлены props: `searchQuery`, `onSearchChange`
- ✅ Убран локальный `useState` для searchQuery
- ✅ Фильтрация упрощена - только по МП
- ✅ Input связан с глобальным state через onSearchChange

### 2. `pages/Projects.tsx`
- ✅ Добавлены props в KanbanBoard: `searchQuery` и `onSearchChange`
- ✅ Теперь Kanban использует `filters.searchQuery` из хука

### 3. `hooks/useProjectsData.ts`
- ⚪ Не изменялся (логика фильтрации уже была правильной)

---

## 🎨 UX улучшения

### До:
❌ Кликаю "В РАБОТЕ" → Grid обновляется, Kanban **НЕ**обновляется  
❌ Ищу в поле Kanban → только Kanban обновляется  
❌ Переключаюсь Grid ↔ Kanban → поиск сбрасывается  

### После:
✅ Кликаю "В РАБОТЕ" → Grid **И** Kanban обновляются  
✅ Ищу в поле Kanban → **все режимы** обновляются  
✅ Переключаюсь Grid ↔ Kanban → поиск **сохраняется**  

---

## 🧪 Как протестировать

1. **Тест быстрых фильтров:**
   - Откройте страницу Projects
   - Кликните "В РАБОТЕ" → должно показать только активные проекты
   - Переключите на Kanban → должны быть те же проекты
   - Кликните "АУДИТ" → должны остаться только проекты в статусе "Аудит"

2. **Тест поиска:**
   - Переключитесь на Kanban
   - Введите ГИС код в поиск Kanban
   - Переключитесь на Grid → поиск должен сохраниться
   - Очистите через "✕ Очистить" → все фильтры сбросятся

3. **Тест фильтра МП (только Kanban):**
   - В Kanban выберите МП из выпадающего списка
   - Должны остаться только проекты выбранного МП
   - Это локальный фильтр, при переключении на Grid он НЕ сохраняется

---

**Дата:** 2026-01-06  
**Статус:** ✅ Фильтры полностью функциональны и синхронизированы  
**Время:** ~15 минут
