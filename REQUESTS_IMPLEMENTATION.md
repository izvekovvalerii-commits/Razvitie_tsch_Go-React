# Внедрение сущности "Заявка"

## 📋 Обзор

В систему добавлена новая сущность **"Заявка"**, позволяющая пользователям создавать запросы информации друг для друга в рамках бизнес-процесса. Заявки автоматически направляются ответственным лицам и проходят полный жизненный цикл от создания до закрытия.

---

## ✅ Реализованные компоненты

### Backend (Go)

#### 1. **Модель данных** (`models/request.go`)
- Структура `Request` с полями:
  - Метаданные: Title, Description, Status, Priority
  - Участники: CreatedByUserId, AssignedToUserId
  - Связи: ProjectId, TaskId (опциональные)
  - Временные метки: CreatedAt, UpdatedAt, TakenAt, AnsweredAt, ClosedAt
  - Дополнительно: Response, DueDate
- Статусы: Новая, В работе, Отвечена, Закрыта, Отклонена
- Приоритеты: Низкий, Средний, Высокий, Срочный
- Валидация на уровне модели

#### 2. **Repository** (`repositories/request_repository.go`)
- CRUD операции
- Фильтрация по создателю/ответственному
- Фильтрация по статусу
- Подсчет статистики (активные, просроченные)
- Предзагрузка связанных данных (Preload)

#### 3. **Service** (`services/request_service.go`)
- Бизнес-логика создания заявок
- Управление статусами (TakeInWork, Answer, Close, Reject)
- Создание уведомлений
- Публикация событий через EventBus

#### 4. **Controller** (`controllers/request_controller.go`)
- RESTful API endpoints:
  - `GET /api/requests` - список заявок с фильтрами
  - `GET /api/requests/:id` - получить заявку
  - `POST /api/requests` - создать заявку
  - `PUT /api/requests/:id` - обновить заявку
  - `DELETE /api/requests/:id` - удалить заявку
  - `PUT /api/requests/:id/take` - взять в работу
  - `PUT /api/requests/:id/answer` - ответить
  - `PUT /api/requests/:id/close` - закрыть
  - `PUT /api/requests/:id/reject` - отклонить
  - `GET /api/requests/stats/:userId` - статистика

#### 5. **События** (`events/definitions.go`)
- `request.created`
- `request.taken`
- `request.answered`
- `request.closed`
- `request.rejected`

#### 6. **Маршруты** (`routes/routes.go`)
- Интеграция контроллера заявок
- Инициализация сервиса
- Все эндпоинты защищены аутентификацией

#### 7. **Миграции** (`database/database.go`)
- Автоматическое создание таблицы `Requests`

---

### Frontend (React + TypeScript)

#### 1. **Типы** (`types/index.ts`)
- Interface `Request`
- Interface `CreateRequestDto`
- Type aliases для статусов и приоритетов

#### 2. **API Сервис** (`services/requests.ts`)
- Полный набор методов для работы с API
- Типизированные запросы/ответы
- Поддержка фильтрации

#### 3. **Страница управления** (`pages/Requests.tsx`)
- Список заявок с фильтрацией
- Создание новых заявок
- Модальные окна для действий
- Real-time обновления
- Разделение на вкладки: Все / Мои заявки / В работу мне
- Фильтрация по статусам

#### 4. **Стили** (`pages/Requests.css`)
- Современный дизайн с градиентами
- Адаптивная верстка
- Анимации и переходы
- Цветовая индикация статусов и приоритетов

#### 5. **Маршрутизация** (`App.tsx`)
- Добавлен маршрут `/requests`
- Защита через ProtectedRoute

---

## 🎨 Дизайн-решения

### Цветовая схема статусов
- **Новая**: Синий (#1976d2)
- **В работе**: Оранжевый (#f57c00)
- **Отвечена**: Фиолетовый (#7b1fa2)
- **Закрыта**: Зеленый (#388e3c)
- **Отклонена**: Красный (#c62828)

### Цветовая схема приоритетов
- **Низкий**: Серый
- **Средний**: Желтый
- **Высокий**: Оранжевый
- **Срочный**: Темно-красный

### UI/UX особенности
- Карточный дизайн для заявок
- Hover-эффекты для интерактивности
- Модальные окна для создания и ответов
- Адаптивная верстка для мобильных устройств
- Плавные анимации

---

## 🔄 Бизнес-процесс

### Жизненный цикл заявки

```
1. Создание → [Новая]
   - Инициатор создает заявку
   - Автоматическое уведомление ответственному

2. Взятие в работу → [В работе]
   - Ответственный начинает работу
   - Уведомление инициатору

3. Ответ → [Отвечена]
   - Ответственный предоставляет информацию
   - Уведомление инициатору

4. Закрытие → [Закрыта]
   - Инициатор подтверждает получение информации
   - Уведомление ответственному

Альтернативный путь:
2. Отклонение → [Отклонена]
   - Ответственный отклоняет с указанием причины
   - Уведомление инициатору
```

---

## 📊 Файловая структура

### Созданные файлы

**Backend:**
```
backend-go/
├── models/request.go                      [NEW]
├── repositories/request_repository.go     [NEW]
├── services/request_service.go            [NEW]
├── controllers/request_controller.go      [NEW]
├── events/definitions.go                  [MODIFIED]
├── routes/routes.go                       [MODIFIED]
└── database/database.go                   [MODIFIED]
```

**Frontend:**
```
frontend-react/src/
├── types/index.ts                         [MODIFIED]
├── services/requests.ts                   [NEW]
├── pages/Requests.tsx                     [NEW]
├── pages/Requests.css                     [NEW]
└── App.tsx                                [MODIFIED]
```

**Документация:**
```
Portal_go_react/
└── REQUESTS_FEATURE.md                    [NEW]
```

---

## 🔒 Безопасность

- ✅ Аутентификация на всех эндпоинтах
- ✅ Валидация входных данных
- ✅ Проверка прав доступа (только инициатор/ответственный может выполнять действия)
- ✅ SQL injection защита через GORM
- ✅ XSS защита на frontend

---

## 🚀 Запуск и тестирование

### 1. Backend
```bash
cd backend-go
go run main.go
```
Backend автоматически создаст таблицу `Requests` при запуске.

### 2 Frontend
```bash
cd frontend-react
npm run dev
```

### 3. Доступ
- Страница заявок: `http://localhost:5174/requests`
- API: `http://localhost:8080/api/requests`

### 4. Тестирование
1. Войдите под пользователем A
2. Создайте заявку для пользователя B
3. Войдите под пользователем B
4. Возьмите заявку в работу
5. Ответьте на заявку
6. Войдите под пользователем A
7. Закройте заявку

---

## 📈 Возможные улучшения

### Краткосрочные
- [ ] Выбор ответственного из списка пользователей (dropdown)
- [ ] Поиск по заявкам
- [ ] Сортировка списка
- [ ] Пагинация

### Среднесрочные
- [ ] Комментарии к заявкам
- [ ] Прикрепление файлов
- [ ] История изменений
- [ ] Email уведомления

### Долгосрочные
- [ ] Автоэскалация просроченных заявок
- [ ] Теги и категории
- [ ] Шаблоны заявок
- [ ] Аналитика и отчеты
- [ ] Массовые операции
- [ ] Интеграция с внешними системами

---

## 📚 API Документация

### Фильтры запроса GET /api/requests

| Параметр | Тип | Описание |
|----------|-----|----------|
| createdBy | number | ID создателя |
| assignedTo | number | ID ответственного |
| status | string | Статус заявки |

### Пример запросов

**Создание заявки:**
```bash
curl -X POST http://localhost:8080/api/requests \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Запрос информации по проекту",
    "description": "Нужны данные по бюджету",
    "priority": "Высокий",
    "createdByUserId": 1,
    "assignedToUserId": 2
  }'
```

**Взятие в работу:**
```bash
curl -X PUT http://localhost:8080/api/requests/1/take \
  -H "Content-Type: application/json" \
  -d '{"userId": 2}'
```

**Ответ на заявку:**
```bash
curl -X PUT http://localhost:8080/api/requests/1/answer \
  -H "Content-Type: application/json" \
  -d '{
    "userId": 2,
    "response": "Бюджет проекта: 5 млн руб."
  }'
```

---

## ✨ Заключение

Система заявок полностью интегрирована в существующую архитектуру приложения и следует установленным паттернам проектирования. Функциональность готова к использованию и может быть легко расширена в будущем.

### Ключевые преимущества:
- 🎯 Полный жизненный цикл заявок
- 🔔 Система уведомлений
- 📊 Фильтрация и статистика
- 🎨 Современный UI/UX
- 🔒 Безопасность и валидация
- 📱 Адаптивный дизайн
- ⚡ Real-time обновления через WebSocket

---

**Разработано:** 2026-01-16  
**Версия системы:** 1.0  
**Backend:** Go + Gin + GORM + PostgreSQL  
**Frontend:** React + TypeScript + CSS
