# Portal Razvitie - Go Backend

Бэкенд портала развития торговых объектов на Go с использованием Gin и GORM.

## 🚀 Технологии

- **Go 1.21+**
- **Gin** - веб-фреймворк
- **GORM** - ORM для работы с PostgreSQL
- **PostgreSQL** - база данных
- **Swagger** - документация API

## 📁 Структура проекта

```
backend-go/
├── config/          # Конфигурация и переменные окружения
├── controllers/     # HTTP контроллеры
├── database/        # Подключение к БД и миграции
├── models/          # Модели данных
├── routes/          # Маршрутизация
└── main.go          # Точка входа
```

## 🔧 Установка и запуск

### 1. Установите зависимости

```bash
cd backend-go
go mod tidy
```

### 2. Настройте переменные окружения

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Отредактируйте `.env` с вашими настройками:

```env
DB_HOST=localhost
DB_PORT=5432
DB_USER=valeriy.izvekov
DB_PASSWORD=
DB_NAME=portal_razvitie

SERVER_PORT=5000
CORS_ORIGIN=http://localhost:4200

UPLOAD_DIR=./uploads
```

### 3. Убедитесь что PostgreSQL запущен

Текущий .NET бэкенд использует базу данных `PortalRazvitie`. Go бэкенд будет использовать ту же базу.

### 4. Запустите сервер

```bash
go run main.go
```

Сервер запустится на `http://localhost:5000`

## 📡 API Endpoints

### Stores (Магазины)
- `GET /api/stores` - Получить все магазины
- `GET /api/stores/:id` - Получить магазин по ID
- `POST /api/stores` - Создать магазин
- `PUT /api/stores/:id` - Обновить магазин
- `DELETE /api/stores/:id` - Удалить магазин

### Projects (Проекты)
- `GET /api/projects` - Получить все проекты
- `GET /api/projects/:id` - Получить проект по ID
- `POST /api/projects` - Создать проект
- `PUT /api/projects/:id` - Обновить проект
- `PATCH /api/projects/:id/status` - Обновить статус проекта
- `DELETE /api/projects/:id` - Удалить проект

### Tasks (Задачи)
- `GET /api/tasks` - Получить все задачи
- `GET /api/tasks/project/:projectId` - Получить задачи проекта
- `POST /api/tasks` - Создать задачу
- `PUT /api/tasks/:id` - Обновить задачу
- `PATCH /api/tasks/:id/status` - Обновить статус задачи
- `DELETE /api/tasks/cleanup-old` - Очистить старые задачи
- `GET /api/tasks/debug-assignments` - Отладка назначений

### Documents (Документы)
- `POST /api/documents/upload` - Загрузить документ
- `GET /api/documents/:id` - Получить документ по ID
- `GET /api/documents/project/:projectId` - Получить документы проекта
- `GET /api/documents/task/:taskId` - Получить документы задачи
- `GET /api/documents/download/:id` - Скачать документ
- `DELETE /api/documents/:id` - Удалить документ

## 🔄 Миграция с .NET

Этот бэкенд **полностью совместим** с текущим .NET API. Все эндпоинты имеют те же URL и возвращают данные в том же формате.

Чтобы переключить фронтенд на Go бэкенд:

1. Остановите .NET сервер
2. Запустите Go сервер на том же порту (5000)
3. Фронтенд будет работать без изменений

## 📦 Сборка для продакшена

```bash
go build -o portal-razvitie main.go
./portal-razvitie
```

## 🧪 Тестирование

Проверьте здоровье сервера:

```bash
curl http://localhost:5000/health
```

## 📝 Особенности реализации

- ✅ Полная совместимость с .NET API
- ✅ Те же структуры данных и валидация
- ✅ Поддержка загрузки файлов
- ✅ CORS настроен для фронтенда на порту 4200
- ✅ Автоматические миграции БД через GORM
- ✅ Использование той же базы данных PostgreSQL

## 🎯 Преимущества Go версии

- **Производительность**: Go быстрее .NET в большинстве сценариев
- **Простота деплоя**: Один бинарный файл без зависимостей
- **Меньше памяти**: Меньше потребление ресурсов
- **Быстрая компиляция**: Мгновенная сборка проекта
- **Встроенная concurrency**: Отличная производительность при высоких нагрузках
