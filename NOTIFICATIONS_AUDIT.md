# Аудит системы уведомлений

## Дата проверки: 2026-01-12

---

## 📊 Общая оценка: 7/10

Система уведомлений работает, но имеет несколько критических и средней важности проблем, требующих внимания.

---

## ✅ Что работает хорошо

### Backend (Go)

1. **Модель данных** ✅
   - Правильно структурированная модель `Notification`
   - Корректные JSON-теги (camelCase)
   - Поля `RelatedProjectID` и `RelatedTaskID` добавлены как nullable

2. **Repository Pattern** ✅
   - Чистая архитектура с интерфейсами
   - Все необходимые CRUD операции
   - Индексы для производительности

3. **WebSocket интеграция** ✅
   - Real-time уведомления через WebSocket Hub
   - Автоматическое переподключение на фронтенде

### Frontend (React/TypeScript)

1. **Хуки** ✅
   - `useNotifications` - чистое разделение логики
   - `useWebSocket` - переподключение при обрыве

2. **UI/UX** ✅
   - Красивый дизайн дропдауна
   - Badge с количеством непрочитанных
   - Анимации и hover-эффекты

---

## 🚨 Критические проблемы

### 1. **Отсутствует обработка query-параметра `editTask` в ProjectDetails**

**Проблема:**
- Навигация `/projects/85?editTask=681` не открывает модальное окно задачи
- Пользователь попадает на страницу проекта, но задача не открывается автоматически

**Решение:**
```typescript
// В ProjectDetails.tsx добавить:
import { useSearchParams } from 'react-router-dom';

const [searchParams, setSearchParams] = useSearchParams();

useEffect(() => {
    const editTaskId = searchParams.get('editTask');
    if (editTaskId && tasks.length > 0) {
        const task = tasks.find(t => t.id === parseInt(editTaskId));
        if (task) {
            setSelectedTask(task);
            setShowEditTaskModal(true);
            // Очистить параметр после открытия
            searchParams.delete('editTask');
            setSearchParams(searchParams, { replace: true });
        }
    }
}, [searchParams, tasks]);
```

**Приоритет:** 🔴 **ВЫСОКИЙ**

---

### 2. **Нет проверки прав доступа на удаление уведомлений**

**Проблема:**
```go
// notification_controller.go, строка 75
func (nc *NotificationController) Delete(c *gin.Context) {
    id, _ := strconv.Atoi(c.Param("id"))
    // ❌ Любой пользователь может удалить любое уведомление по ID
    if err := nc.notifService.Delete(uint(id)); err != nil {
```

**Решение:**
```go
func (nc *NotificationController) Delete(c *gin.Context) {
    id, _ := strconv.Atoi(c.Param("id"))
    userID := GetCurrentUserID(c) // Из JWT/сессии
    
    // Проверяем, что уведомление принадлежит текущему пользователю
    notification, err := nc.notifService.GetByID(uint(id))
    if err != nil || notification.UserID != userID {
        c.JSON(http.StatusForbidden, gin.H{"error": "Access denied"})
        return
    }
    
    if err := nc.notifService.Delete(uint(id)); err != nil {
        ...
    }
}
```

**Приоритет:** 🔴 **ВЫСОКИЙ** (безопасность)

---

## ⚠️ Проблемы средней важности

### 3. **Отсутствует обработка ошибок при конвертации строк**

**Проблема:**
```go
// notification_controller.go, множество мест
userID, _ := strconv.Atoi(userIdStr)  // ❌ Игнорируем ошибку
```

**Решение:**
```go
userID, err := strconv.Atoi(userIdStr)
if err != nil {
    c.JSON(http.StatusBadRequest, gin.H{"error": "Invalid userId"})
    return
}
```

**Приоритет:** 🟡 **СРЕДНИЙ**

---

### 4. **Хардкод URL WebSocket**

**Проблема:**
```typescript
// useWebSocket.ts:3
const WS_URL = 'ws://localhost:5000/ws';  // ❌ Не работает в продакшене
```

**Решение:**
```typescript
const WS_URL = import.meta.env.VITE_WS_URL || 
    `ws://${window.location.hostname}:5000/ws`;
```

**Приоритет:** 🟡 **СРЕДНИЙ**

---

### 5. **Нет пагинации уведомлений**

**Проблема:**
- Загружается фиксированный лимит в 50 уведомлений
- Старые уведомления недоступны

**Решение:**
- Реализовать infinite scroll или пагинацию
- Добавить параметры `offset`/`limit` в API

**Приоритет:** 🟡 **СРЕДНИЙ**

---

### 6. **Отсутствует Title в уведомлениях**

**Проблема:**
```typescript
// useNotifications.ts - Title не используется
const mapped = data.notifications.map((n: any) => ({
    ...
    // ❌ title теряется
}));
```

**Бэкенд отправляет:**
```json
{
    "title": "Новый комментарий",
    "message": "К вашей задаче..."
}
```

**Но фронтенд не использует `title`**

**Решение:**
- Добавить `title` в интерфейс `Notification`
- Отображать title в UI как заголовок

**Приоритет:** 🟢 **НИЗКИЙ**

---

## 🔧 Рекомендации по улучшению

### 7. **Добавить типизацию для типов уведомлений**

```typescript
// types/index.ts
export enum NotificationType {
    COMMENT = 'COMMENT',
    TASK_ASSIGNED = 'TASK_ASSIGNED',
    TASK_STATUS_CHANGED = 'TASK_STATUS_CHANGED',
    PROJECT_CREATED = 'PROJECT_CREATED',
    SYSTEM = 'SYSTEM'
}

export interface Notification {
    id: number;
    title: string;
    message: string;
    type: NotificationType;
    ...
}
```

---

### 8. **Группировка уведомлений**

Например: "3 новых комментария к вашей задаче" вместо 3 отдельных уведомлений.

---

### 9. **Push-уведомления браузера**

Для важных уведомлений (срок задачи истекает) - использовать Notifications API.

---

### 10. **Настройки уведомлений для пользователя**

Разрешить отключать определенные типы уведомлений.

---

## 📋 План действий (приоритизированный)

### Немедленно:
1. ✅ Добавить обработку `editTask` в ProjectDetails
2. ✅ Добавить проверку прав на удаление уведомлений
3. ✅ Исправить обработку ошибок конвертации строк

### Краткосрочно (неделя):
4. Вынести WS_URL в переменные окружения
5. Добавить поле `title` в маппинг уведомлений
6. Добавить тесты для NotificationService

### Долгосрочно (месяц):
7. Реализовать пагинацию
8. Добавить группировку уведомлений
9. Настройки уведомлений для пользователей

---

## 🏗️ Архитектурные замечания

### Положительное:
- ✅ Чистая архитектура (Repository → Service → Controller)
- ✅ Разделение ответственности
- ✅ Real-time через WebSocket
- ✅ Типизация на фронтенде

### Что можно улучшить:
- ⚠️ Добавить middleware для аутентификации в контроллерах
- ⚠️ Вынести бизнес-логику из listeners в сервисы
- ⚠️ Добавить rate limiting для CreateNotification
- ⚠️ Логирование важных событий (создание/удаление уведомлений)

---

## 📈 Метрики для мониторинга

Рекомендую добавить:
1. Время доставки уведомления (от создания до получения)
2. Процент прочитанных уведомлений
3. Количество WebSocket переподключений
4. Частота использования функции "Очистить все"

---

## Заключение

Система уведомлений реализована качественно с точки зрения архитектуры, но требует доработки в части:
- **Безопасности** (проверка прав доступа)
- **UX** (автооткрытие задачи по уведомлению)
- **Обработки ошибок**

После исправления критических проблем система будет готова к продакшену.
