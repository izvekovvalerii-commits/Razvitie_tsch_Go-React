# 🏗️ Стратегия Оптимизации Архитектуры и Кода

Этот документ содержит детальный анализ текущего состояния проекта и дорожную карту по приведению его к лучшим практикам индустрии (Best Practices).

---

## 🔍 Текущее состояние (Analysis)

### Backend (Go)
**✅ Сильные стороны:**
- Использование **GORM** для работы с БД.
- **Graceful Shutdown** реализован корректно.
- Структурированное логирование (ZeroLog/Logger).
- Конфигурация вынесена отдельно.

**⚠️ Зоны роста:**
- **Global State:** Использование глобальной переменной `database.DB` нарушает принцип инверсии зависимости (Dependency Inversion).
- **Service Initialization:** "Ручная" сборка сервисов через сеттеры может привести к ошибкам (недоинициализированный сервис).
- **Testing:** Отсутствие unit-тестов для бизнес-логики.
- **CORS:** Жестко прописанные настройки CORS.

### Frontend (React)
**✅ Сильные стороны:**
- Использование **TypeScript** (строгая типизация).
- Понятная структура папок (`components`, `pages`, `hooks`).
- RBAC (Role Based Access Control) реализован.
- Разделение бизнес-логики в кастомные хуки (`useProjectsData`).

**⚠️ Зоны роста:**
- **Data Fetching:** Использование "сырого" `useEffect` + `fetch` для загрузки данных.
  - *Последствия:* "Водопады" загрузок, мерцание UI, отсутствие кэширования, риск "race conditions", ручная обработка `loading/error`.
- **State Management:** Фильтры и данные размазаны между `Projects.tsx`, хуками и `KanbanBoard`.
- **Component Size:** Крупные компоненты (Monoliths), содержащие и UI, и логику.
- **Performance:** Частые ререндеры из-за отсутствия `useCallback/useMemo` в критических местах.

---

## 🚀 План Оптимизации (Roadmap)

### Этап 1: Модернизация Frontend (High Impact)
*Цель: Улучшить отзывчивость интерфейса, упростить код, убрать баги состояния.*

#### 1. Внедрение TanStack Query (React Query) 🔥
Это **самое важное улучшение** для фронтенда.
- **Зачем:** Заменяет тонны кода `useEffect`, `useState(loading)`, `useState(error)`. Дает кэширование, deduping запросов, refetch on window focus.
- **Действие:**
  - Установить `@tanstack/react-query`.
  - Переписать `useProjectsData` с использованием `useQuery`.
  - Переписать мутации (create/update/delete) на `useMutation` с автоматической инвалидацией кэша (список обновляется сам).

#### 2. Глобальный стейт через Zustand
- **Зачем:** Чтобы фильтры ("В работе", "searchQuery") были доступны везде без Prop Drilling, но проще, чем Redux.
- **Действие:** Создать стор `useFilterStore` для управления фильтрами проектов.

#### 3. Atomic Design & UI Kit
- **Зачем:** Убрать дублирование стилей и верстки.
- **Действие:** Выделить базовые компоненты в `src/components/ui`:
  - `Button`, `Input`, `Badge`, `Modal`.

---

### Этап 2: Чистота Архитектуры Backend (Architecture Check)
*Цель: Сделать код тестируемым и поддерживаемым.*

#### 1. Pure Dependency Injection
- **Зачем:** Чтобы легко мокать БД в тестах.
- **Действие:**
  - Убрать глобальный `database.DB`.
  - Передавать `*gorm.DB` (или интерфейс `Datastore`) через конструкторы `New...`.
  - Сделать конструкторы `NewService` обязательными, принимающими все зависимости.

#### 2. Service Layer Isolation
- **Зачем:** Контроллеры не должны знать про GORM.
- **Действие:** Убедиться, что Controller оперирует только DTO (Data Transfer Objects), а Service возвращает доменные модели или ошибки.

#### 3. Structured Error Handling
- **Зачем:** Чтобы фронтенд получал понятные коды ошибок, а логи — стектрейсы.
- **Действие:** Внедрить кастомные ошибки (Sentinel Errors) в слое сервисов.

---

### Этап 3: Инфраструктура и Контейнеризация
*Цель: Dev/Prod parity.*

#### 1. Docker Compose Evolution
- **Зачем:** Одна команда для запуска всего стека.
- **Действие:** Настроить `docker-compose.yml` с hot-reload для фронта (Vite) и бэка (Air).

#### 2. Nginx Gateway
- **Зачем:** Решить проблемы CORS и маршрутизации раз и навсегда.
- **Действие:** Поставить Nginx перед Бэком и Фронтом.

---

## 💡 Immediate Action Items (Что делать прямо сейчас?)

Рекомендую начать с **Frontend**, так как это даст наибольшую видимую пользу пользователю.

1.  **Установить React Query**:
    `npm i @tanstack/react-query @tanstack/react-query-devtools`
2.  **Настроить QueryClientProvider** в `main.tsx`.
3.  **Рефакторинг `useProjectsData`**: Заменить ручной fetch на `useQuery`.

Это сразу решит проблемы с синхронизацией данных между Kanban/Table и уберет лишние ререндеры.
