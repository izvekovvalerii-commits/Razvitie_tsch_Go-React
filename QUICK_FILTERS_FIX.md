# ✅ Исправление логики быстрых фильтров

## 🔴 Проблема
Фильтры работали некорректно ("почти нефункциональны"), так как использовали неправильные статусы и устаревшую логику:
1. Фильтр **"Аудит"** искал статус `'Аудит'`, которого нет в системе (есть `'Подготовка к аудиту'` и `'Аудит объекта'`). Из-за этого он всегда показывал 0 результатов.
2. Фильтр **"В работе"** включал статус `'Открыт'`, что логически неверно для активных задач разработки/открытия.
3. Счетчики на кнопках не совпадали с результатами фильтрации.

## ✅ Решение

### 1. Обновил логику в `useProjectsData.ts`

**Было:**
```typescript
if (quickFilter === 'active') {
    result = result.filter(p => !['Слетел', 'Закрыт', 'Архив'].includes(p.status));
} else if (quickFilter === 'audit') {
    result = result.filter(p => p.status === 'Аудит'); // ❌ Ошибка
}
```

**Стало:**
```typescript
if (quickFilter === 'active') {
    // Исключаем 'Открыт', так как это целевое состояние успеха
    result = result.filter(p => !['Открыт', 'Закрыт', 'Архив', 'Слетел'].includes(p.status));
} else if (quickFilter === 'audit') {
    // Используем правильные статусы из констант
    result = result.filter(p => ['Подготовка к аудиту', 'Аудит объекта'].includes(p.status));
}
```

### 2. Синхронизировал счетчики в `Projects.tsx`

**Было:**
```typescript
const auditCount = projects.filter(p => p.status === 'Аудит').length;
```

**Стало:**
```typescript
const auditCount = projects.filter(p => ['Подготовка к аудиту', 'Аудит объекта'].includes(p.status)).length;
```

## 🎯 Результат
Теперь кнопки работают интуитивно понятно:
- **ВСЕГО:** Показывает полный список.
- **В РАБОТЕ:** Активные проекты (в процессе открытия/реконструкции).
- **АУДИТ:** Проекты на стадии аудита (найдет их по правильным статусам).

**Файлы изменены:**
- `hooks/useProjectsData.ts`
- `pages/Projects.tsx`

**Статус:** ✅ Исправлено
