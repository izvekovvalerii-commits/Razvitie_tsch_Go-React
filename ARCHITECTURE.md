# 🏗️ Архитектура Portal Go + React

```
┌─────────────────────────────────────────────────────────────────────┐
│                           FRONTEND (React)                           │
│                         Port: 5174 (Vite)                           │
└─────────────────────────────────────────────────────────────────────┘
                                    │
                            /api proxy (vite.config.ts)
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────┐
│                        BACKEND (Go - Gin)                            │
│                           Port: 8080                                 │
│                                                                       │
│  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐  │
│  │   Controllers    │  │   Middleware     │  │    Services      │  │
│  ├──────────────────┤  ├──────────────────┤  ├──────────────────┤  │
│  │ Projects         │  │ ErrorHandler     │  │ WorkflowService  │  │
│  │ Tasks            │  │ Recovery         │  │ - Generate Tasks │  │
│  │ Stores           │  │ CORS             │  │ - Validate       │  │
│  │ Documents        │  └──────────────────┘  │ - Process        │  │
│  └──────────────────┘                        └──────────────────┘  │
│                                │                                     │
│                                ▼                                     │
│                        ┌──────────────┐                             │
│                        │     GORM     │                             │
│                        └──────────────┘                             │
└──────────────────────────────┬───────────────────────────────────────┘
                               │
                               ▼
                    ┌──────────────────────┐
                    │   PostgreSQL 14      │
                    │  portal_razvitie     │
                    │                      │
                    │  Tables:             │
                    │  - Projects          │
                    │  - ProjectTasks      │
                    │  - ProjectDocuments  │
                    │  - Stores            │
                    └──────────────────────┘
```

## 📦 Слои приложения

### 1. Presentation Layer (Frontend)

```
src/
├── pages/                    # Страницы (роутинг)
│   ├── Projects.tsx         # /projects
│   ├── ProjectDetails.tsx   # /projects/:id
│   ├── Stores.tsx           # /stores
│   └── Home.tsx             # /
│
├── components/              # Переиспользуемые компоненты
│   ├── GanttChart/         # Диаграмма Ганта
│   ├── Header/             # Шапка сайта
│   ├── Footer/             # Подвал
│   └── ProjectDetails/     # Подкомпоненты деталей проекта
│       ├── hooks/          # Бизнес-логика
│       ├── Modals/         # Модальные окна
│       └── TasksList/      # Список задач
│
├── services/               # API клиенты
│   ├── projects.ts        # CRUD проектов
│   ├── tasks.ts           # CRUD задач
│   ├── stores.ts          # CRUD магазинов
│   └── workflow.ts        # BPMN конфигурация
│
├── hooks/                 # React hooks
│   └── useApi.ts         # Централизованные API вызовы
│
├── types/                # TypeScript типы
│   └── index.ts
│
└── constants/           # Константы
    └── store-opening-process.config.ts
```

### 2. API Layer (Backend)

```
backend-go/
├── controllers/         # HTTP handlers
│   ├── projects_controller.go
│   ├── tasks_controller.go
│   ├── stores_controller.go
│   └── documents_controller.go
│
├── services/           # Бизнес-логика
│   └── workflow_service.go
│
├── middleware/         # HTTP middleware
│   └── error_handler.go
│
├── models/            # Data models (GORM)
│   ├── project.go
│   ├── task.go
│   ├── store.go
│   └── document.go
│
├── routes/           # Роутинг
│   └── routes.go
│
├── database/         # БД подключение
│   └── database.go
│
├── config/          # Конфигурация
│   └── config.go
│
└── main.go         # Entry point
```

### 3. Data Layer

```sql
-- PostgreSQL Database: portal_razvitie

-- Магазины (справочник)
CREATE TABLE "Stores" (
    "Id" SERIAL PRIMARY KEY,
    "GisCode" VARCHAR(50),
    "Name" VARCHAR(255),
    "Region" VARCHAR(100),
    "Address" TEXT,
    "Format" VARCHAR(50),
    "CFO" VARCHAR(100)
);

-- Проекты открытия
CREATE TABLE "Projects" (
    "Id" SERIAL PRIMARY KEY,
    "StoreId" INT REFERENCES "Stores"("Id"),
    "ProjectType" VARCHAR(50) NOT NULL,
    "Status" VARCHAR(50) DEFAULT 'Создан',
    "GisCode" VARCHAR(50),
    "CreatedAt" TIMESTAMP,
    "UpdatedAt" TIMESTAMP
);

-- Задачи проекта
CREATE TABLE "ProjectTasks" (
    "Id" SERIAL PRIMARY KEY,
    "ProjectId" INT REFERENCES "Projects"("Id"),
    "Code" VARCHAR(50),              -- TASK-PREP-AUDIT
    "Name" VARCHAR(255) NOT NULL,
    "Status" VARCHAR(50),            -- Назначена, В работе, Завершена
    "Responsible" VARCHAR(100),      -- МП, МРиЗ, БА
    "TaskType" VARCHAR(50),          -- UserTask
    "Stage" VARCHAR(50),             -- Инициализация, Аудит, Бюджет
    "IsActive" BOOLEAN,              -- Может быть начата
    "NormativeDeadline" TIMESTAMP,
    "ActualDate" TIMESTAMP,
    "CreatedAt" TIMESTAMP
);

-- Документы проекта
CREATE TABLE "ProjectDocuments" (
    "Id" SERIAL PRIMARY KEY,
    "ProjectId" INT REFERENCES "Projects"("Id"),
    "TaskId" INT REFERENCES "ProjectTasks"("Id"),
    "Name" VARCHAR(255) NOT NULL,
    "Type" VARCHAR(100),
    "FilePath" TEXT,
    "Size" BIGINT,
    "UploadDate" TIMESTAMP
);
```

---

## 🔄 Data Flow

### Пример: Создание проекта

```
User Action: Click "Создать проект"
    │
    ▼
┌─────────────────────────────────────────────────────────────────┐
│ 1. Frontend (Projects.tsx)                                      │
│    const { execute } = useApi(projectsService.createProject)    │
│    await execute(newProject)                                    │
└─────────────────────────────────────────────────────────────────┘
    │
    │ HTTP POST /api/projects { storeId, projectType, ... }
    ▼
┌─────────────────────────────────────────────────────────────────┐
│ 2. Backend Middleware                                            │
│    - CORS check                                                  │
│    - Recovery (panic handler)                                    │
└─────────────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────────────┐
│ 3. Controller (projects_controller.go)                          │
│    func CreateProject(c *gin.Context)                           │
│    - Валидация: c.ShouldBindJSON(&project)                      │
│    - Создание: database.DB.Create(&project)                     │
└─────────────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────────────┐
│ 4. Service (workflow_service.go)                                │
│    GenerateProjectTasks(projectID, createdAt)                   │
│    - Читает BPMN конфигурацию (13 задач)                       │
│    - Рассчитывает даты с учетом зависимостей                   │
│    - Создает задачи в БД                                        │
│    - Первые задачи: Status = "Назначена", IsActive = true      │
│    - Остальные: Status = "Ожидание", IsActive = false          │
└─────────────────────────────────────────────────────────────────┘
    │
    ▼
┌─────────────────────────────────────────────────────────────────┐
│ 5. Database (PostgreSQL)                                         │
│    INSERT INTO "Projects" ...                                    │
│    INSERT INTO "ProjectTasks" ... (x13)                         │
└─────────────────────────────────────────────────────────────────┘
    │
    │ JSON Response: { id: 48, tasks: [...] }
    ▼
┌─────────────────────────────────────────────────────────────────┐
│ 6. Frontend                                                      │
│    - useApi обновляет state                                      │
│    - Navigate to /projects/48                                    │
│    - Показывает Gantt Chart с задачами                          │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🎯 Ключевые паттерны

### 1. Repository Pattern (в планах)
Сейчас контроллеры напрямую работают с БД. Можно улучшить:
```go
// repositories/project_repository.go
type ProjectRepository interface {
    FindAll() ([]models.Project, error)
    FindByID(id uint) (*models.Project, error)
    Create(project *models.Project) error
}
```

### 2. Service Layer ✅
Бизнес-логика вынесена в сервисы:
```go
// services/workflow_service.go
type WorkflowService struct {}
func (s *WorkflowService) GenerateProjectTasks(...) {...}
```

### 3. Error Handling ✅
Централизованная обработка через middleware:
```go
// middleware/error_handler.go
func ErrorHandler() gin.HandlerFunc {...}
```

### 4. Validation ✅
Декларативная валидация через binding tags:
```go
type Project struct {
    ProjectType string `binding:"required,oneof=Открытие Реконструкция"`
}
```

---

## 📊 Performance

### Оптимизации:

1. **Frontend**:
   - ✅ React.useMemo для тяжелых вычислений (Gantt)
   - ✅ Lazy loading компонентов
   - ⏳ React Query для кэширования API

2. **Backend**:
   - ✅ GORM Preload для избежания N+1 запросов
   - ✅ Индексы на внешние ключи
   - ⏳ Connection pooling
   - ⏳ Redis кэш для справочников

3. **Database**:
   - ✅ Индексы на FK (StoreId, ProjectId)
   - ⏳ Партиционирование больших таблиц
   - ⏳ Materialized views для отчетов

---

## 🔐 Security Layers

```
┌─────────────────────────────────────────┐
│ 1. Input Validation (binding tags)      │ ✅
├─────────────────────────────────────────┤
│ 2. CORS Protection                      │ ✅
├─────────────────────────────────────────┤
│ 3. Error Masking (no stack traces)     │ ✅
├─────────────────────────────────────────┤
│ 4. JWT Authentication                   │ ⏳
├─────────────────────────────────────────┤
│ 5. RBAC Authorization                   │ ⏳
├─────────────────────────────────────────┤
│ 6. Rate Limiting                        │ ⏳
├─────────────────────────────────────────┤
│ 7. SQL Injection Protection (GORM)     │ ✅
└─────────────────────────────────────────┘
```

---

Полная документация в `SERVICES_OVERVIEW.md`
