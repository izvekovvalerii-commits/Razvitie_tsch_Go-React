# Динамический пересчет сроков задач

Дата: 2026-01-02
Статус: ✅ Реализовано

## Описание функции

Плановые сроки задач автоматически пересчитываются, когда предыдущие задачи завершаются раньше или позже планового срока.

## Как работает

### Сценарий 1: Задача завершена раньше срока

**Пример:**
```
Задача A: "Подготовка к аудиту"
├─ План: 1 янв - 3 янв (2 дня)
└─ Факт: Завершена 2 янв ✅ (на 1 день раньше!)

Задача B: "Аудит объекта" (зависит от A)
├─ Старый план: 4 янв - 5 янв
└─ Новый план: 3 янв - 4 янв ⏩ (сдвинулась на 1 день раньше)
```

### Сценарий 2: Задача завершена позже срока

**Пример:**
```
Задача A: "Подготовка к аудиту"
├─ План: 1 янв - 3 янв (2 дня)
└─ Факт: Завершена 5 янв ⏳ (на 2 дня позже)

Задача B: "Аудит объекта" (зависит от A)
├─ Старый план: 4 янв - 5 янв
└─ Новый план: 6 янв - 7 янв ⏭️ (сдвинулась на 2 дня позже)
```

### Сценарий 3: Множественные зависимости

**Пример:**
```
Задача A: Завершена 10 янв
Задача B: Завершена 12 янв
Задача C: Завершена 8 янв

Задача D (зависит от A, B, C):
└─ Новый старт: 13 янв (max(10, 12, 8) + 1 день = 12 + 1 = 13)
└─ Новый дедлайн: 13 янв + duration
```

## Реализация

### Файл: `workflow_service.go`

```go
func (s *WorkflowService) ProcessTaskCompletion(projectID uint, completedTaskCode string) error {
    // 1. Загружаем все задачи проекта
    var projectTasks []models.ProjectTask
    database.DB.Where("\"ProjectId\" = ?", projectID).Find(&projectTasks)
    
    // 2. Для каждой ожидающей задачи
    for _, def := range StoreOpeningTasks {
        if status != "Ожидание" {
            continue
        }
        
        // 3. Проверяем зависимости
        var maxActualDate time.Time
        hasActualDates := false
        
        for _, depCode := range def.DependsOn {
            // Если зависимость завершена
            for _, depTask := range projectTasks {
                if depTask.ActualDate != nil {
                    hasActualDates = true
                    if depTask.ActualDate.After(maxActualDate) {
                        maxActualDate = *depTask.ActualDate
                    }
                }
            }
        }
        
        // 4. Если все зависимости выполнены
        if allDepsMet {
            // 5. Пересчитываем срок
            if hasActualDates {
                newStart := maxActualDate.AddDate(0, 0, 1)  // Следующий день после последней завершенной
                newDeadline := newStart.AddDate(0, 0, def.Duration)  // + Длительность задачи
                
                task.NormativeDeadline = newDeadline
                
                log.Printf("[Workflow] Task %s deadline recalculated: %s -> %s",
                    def.Code,
                    oldDeadline.Format("2006-01-02"),
                    newDeadline.Format("2006-01-02"))
            }
            
            // 6. Сохраняем и отправляем уведомление
            database.DB.Save(&task)
            s.notifService.SendNotification(...)
        }
    }
}
```

## Логика расчета

### Формулы:

1. **Новая дата старта:**
   ```
   NewStart = MAX(ActualDate всех зависимостей) + 1 день
   ```

2. **Новый дедлайн:**
   ```
   NewDeadline = NewStart + Duration задачи
   ```

### Пример расчета:

```javascript
// Зависимости:
dep1.actualDate = 2026-01-05
dep2.actualDate = 2026-01-03
dep3.actualDate = 2026-01-07

// Расчет:
maxActualDate = max(2026-01-05, 2026-01-03, 2026-01-07) = 2026-01-07
newStart = 2026-01-07 + 1 день = 2026-01-08
newDeadline = 2026-01-08 + 2 дня (Duration) = 2026-01-10
```

## Логирование

При каждом пересчете в логах появляется запись:

```log
[Workflow] Task TASK-AUDIT deadline recalculated: 2026-01-04 -> 2026-01-03 (based on actual completion of dependencies)
```

### Формат:
```
[Workflow] Task {CODE} deadline recalculated: {OLD} -> {NEW} (based on actual completion of dependencies)
```

## Тестирование

### Сценарий тестирования:

1. **Создать проект** с магазином "Тестовый"
2. **Проверить плановые сроки:**
   ```
   TASK-PREP-AUDIT: 02.01.2026 - 04.01.2026 (2 дня)
   TASK-AUDIT: 05.01.2026 - 06.01.2026 (1 день) ⏱️ план
   ```

3. **Завершить задачу раньше:**
   - Завершить `TASK-PREP-AUDIT` сегодня (02.01.2026)
   - Ожидаемое: завершение на 2 дня раньше

4. **Проверить новые сроки:**
   ```
   TASK-AUDIT: 03.01.2026 - 04.01.2026 (1 день) ⏩ сдвинулось на 2 дня раньше!
   ```

5. **Проверить логи:**
   ```
   [Workflow] Processing completion for task TASK-PREP-AUDIT in project 73
   [Workflow] Activating task TASK-AUDIT
   [Workflow] Task TASK-AUDIT deadline recalculated: 2026-01-06 -> 2026-01-04
   ✅ Notification sent for task TASK-AUDIT to user 3
   ```

## Польза для бизнеса

### Преимущества:

1. **Реалистичное планирование** 📊
   - Сроки автоматически адаптируются к фактическому темпу работы
   - Нет необходимости вручную корректировать план

2. **Ускорение проекта** ⚡
   - Если команда работает быстрее - весь проект ускоряется
   - Команда может видеть реальные, а не устаревшие даты

3. **Точные дедлайны** 🎯
   - Ответственные видят актуальные сроки
   - Меньше путаницы с "а когда мне начинать?"

4. **Автоматизация** 🤖
   - Система сама пересчитывает - не нужно делать это вручную
   - Меньше ошибок планирования

## Интеграция с Gantt Chart

Диаграмма Ганта автоматически отображает:
- ✅ Обновленные плановые сроки
- ✅ Фактические даты завершения
- ✅ Связи между задачами

## Будущие улучшения

### Возможные дополнения:

1. **История изменений** 📜
   - Сохранять старые и новые даты в таблицу
   - Показывать "было → стало" в UI

2. **Уведомления об изменениях** 🔔
   - "Ваша задача теперь начинается раньше!"
   - "Дедлайн сдвинулся на 2 дня вперед"

3. **Влияние на весь проект** 📈
   - Показывать как изменение одной задачи влияет на весь проект
   - "Проект завершится на 5 дней раньше!"

4. **Критический путь** 🛣️
   - Подсвечивать задачи которые влияют на общий срок проекта
   - Приоритизация критических задач

## Технические детали

### Зависимости:
- `workflow_service.go` - логика пересчета
- `task_service.go` - вызывает ProcessTaskCompletion
- `models.ProjectTask` - поля ActualDate, NormativeDeadline

### Поля используемые:
- `ActualDate` - фактическая дата завершения
- `NormativeDeadline` - плановый срок (пересчитывается)
- `Duration` - длительность задачи (не меняется)
- `DependsOn` - зависимости задачи

### Performance:
- O(n*m) где n = количество задач, m = количество зависимостей
- Для типичного проекта (13 задач) - мгновенно

## Заключение

Функция динамического пересчета сроков делает планирование гибким и адаптивным,
автоматически корректируя план на основе реальной скорости выполнения работ.

**Статус:** ✅ Работает  
**Тестирование:** ✅ Готово  
**Документация:** ✅ Полная  
**В продакшене:** 🚀 Готово к деплою
